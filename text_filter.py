#! /usr/bin/env python3

for line in open( "./first.py" ):  # don't forget colon at the end.
  print( line, end='' )            # end='' suppresses newline at the end.
                                   # each line has new line already.
                                   # also indentation matters. be careful.
                                   
